{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Windgram site context, site-context.json",
  "type": "object",
  "properties": {
    "schemaVersion": {
      "type": "number",
      "const": 2
    },
    "generatedAt": {
      "type": "string",
      "format": "date-time",
      "pattern": "^(?:(?:\\d\\d[2468][048]|\\d\\d[13579][26]|\\d\\d0[48]|[02468][048]00|[13579][26]00)-02-29|\\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\\d|30)|(?:02)-(?:0[1-9]|1\\d|2[0-8])))T(?:(?:[01]\\d|2[0-3]):[0-5]\\d(?::[0-5]\\d(?:\\.\\d+)?)?(?:Z))$",
      "description": "When the context was generated, UTC."
    },
    "sources": {
      "minItems": 1,
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
            "description": "Lowercase hyphenated slug — the identity of models and sites everywhere."
          },
          "product": {
            "type": "string",
            "minLength": 1,
            "description": "Human-readable product name and vintage."
          },
          "kind": {
            "type": "string",
            "enum": [
              "surfaceModel",
              "bareEarthModel",
              "landCover"
            ],
            "description": "What the source measures: \"surfaceModel\" = DSM, canopy and buildings included; \"bareEarthModel\" = DTM, ground returns only; \"landCover\" = classified cover."
          },
          "resolutionM": {
            "type": "number",
            "exclusiveMinimum": 0,
            "description": "Native ground resolution, metres."
          },
          "licence": {
            "type": "string",
            "minLength": 1,
            "description": "Licence name (e.g. \"CC-BY 4.0\", \"OGL-BC\")."
          },
          "attribution": {
            "type": "string",
            "minLength": 1,
            "description": "The attribution statement the source's licence requires. It travels with the data; renderers displaying this source's values display it."
          },
          "url": {
            "type": "string",
            "minLength": 1,
            "description": "The source's authoritative landing page."
          }
        },
        "required": [
          "id",
          "product",
          "kind",
          "resolutionM",
          "licence",
          "attribution",
          "url"
        ],
        "description": "One upstream data source, with the licence attribution that must travel with its values."
      },
      "description": "Every upstream source any site block references, with licence attributions."
    },
    "sites": {
      "type": "object",
      "propertyNames": {
        "type": "string",
        "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
        "description": "Lowercase hyphenated slug — the identity of models and sites everywhere."
      },
      "additionalProperties": {
        "type": "object",
        "properties": {
          "elevation": {
            "type": "object",
            "properties": {
              "source": {
                "type": "string",
                "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
                "description": "The sources[] entry the pick came from."
              },
              "elevationM": {
                "type": "number",
                "description": "The picked ground elevation at the catalogued coordinates, metres MSL, bilinear."
              }
            },
            "required": [
              "source",
              "elevationM"
            ],
            "description": "THE launch elevation: a measurement selection, not a computation — the pipeline samples ground observations at the catalogued coordinates and picks by explicit priority: lidarbc 1 m ground returns → mrdem30 30 m national DTM → glo30 surface model as a loud last resort (canopy included)."
          },
          "terrain": {
            "type": "object",
            "properties": {
              "source": {
                "type": "string",
                "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
                "description": "The sources[] entry these values came from."
              },
              "elevationM": {
                "type": "number",
                "description": "Terrain-model elevation at the catalogued point, metres MSL, bilinear. From a surface model this includes canopy — compare with the elevation pick before reading small differences as error (a gap over ~100 m suggests the pin hits different terrain in different sources)."
              },
              "slopeDeg": {
                "type": "number",
                "minimum": 0,
                "description": "Terrain slope at the launch, degrees (Horn 3×3 on the source grid)."
              },
              "aspectDeg": {
                "type": "number",
                "minimum": 0,
                "maximum": 359,
                "description": "Compass bearing of the downslope direction, degrees 0-359. Low-confidence on near-summit launches (relief percentile near 100), where noise swings the bearing."
              },
              "relief": {
                "minItems": 1,
                "type": "array",
                "items": {
                  "type": "object",
                  "properties": {
                    "radiusKm": {
                      "type": "number",
                      "exclusiveMinimum": 0
                    },
                    "minM": {
                      "type": "number",
                      "description": "Lowest terrain in the disc, metres MSL."
                    },
                    "maxM": {
                      "type": "number",
                      "description": "Highest terrain in the disc, metres MSL."
                    },
                    "percentile": {
                      "type": "number",
                      "minimum": 0,
                      "maximum": 100,
                      "description": "The launch elevation's percentile rank among the disc's terrain: 100 = the local summit, 50 = mid-slope. Read radii together — high at 1 km and low at 10 km is a foothill in front of bigger terrain."
                    }
                  },
                  "required": [
                    "radiusKm",
                    "minM",
                    "maxM",
                    "percentile"
                  ],
                  "description": "Terrain relief within one radius of the launch."
                },
                "description": "Relief discs, ascending radius."
              }
            },
            "required": [
              "source",
              "elevationM",
              "slopeDeg",
              "aspectDeg",
              "relief"
            ],
            "description": "Terrain analysis from ONE consistent elevation model across every site, so numbers compare across the catalogue."
          },
          "landCover": {
            "type": "object",
            "properties": {
              "source": {
                "type": "string",
                "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
                "description": "The sources[] entry these values came from."
              },
              "atLaunch": {
                "type": "string",
                "enum": [
                  "treeCover",
                  "shrubland",
                  "grassland",
                  "cropland",
                  "builtUp",
                  "bareSparse",
                  "snowIce",
                  "water",
                  "wetland",
                  "mangroves",
                  "mossLichen"
                ],
                "description": "The class of the single pixel under the launch point. One 10 m pixel is fragile — read it beside the 1 km fractions."
              },
              "fractions": {
                "minItems": 1,
                "type": "array",
                "items": {
                  "type": "object",
                  "properties": {
                    "radiusKm": {
                      "type": "number",
                      "exclusiveMinimum": 0
                    },
                    "byClass": {
                      "type": "object",
                      "propertyNames": {
                        "type": "string",
                        "enum": [
                          "treeCover",
                          "shrubland",
                          "grassland",
                          "cropland",
                          "builtUp",
                          "bareSparse",
                          "snowIce",
                          "water",
                          "wetland",
                          "mangroves",
                          "mossLichen"
                        ],
                        "description": "Land-cover class, the ESA WorldCover taxonomy published as semantic names (10 treeCover, 20 shrubland, 30 grassland, 40 cropland, 50 builtUp, 60 bareSparse, 70 snowIce, 80 water, 90 wetland, 95 mangroves, 100 mossLichen)."
                      },
                      "additionalProperties": {
                        "type": "number",
                        "minimum": 0,
                        "maximum": 1
                      },
                      "description": "Fraction of the disc under each class, 0-1. Classes absent from the disc are omitted — absence means zero here (the map is wall-to-wall), unlike data absences elsewhere in the contract."
                    }
                  },
                  "required": [
                    "radiusKm",
                    "byClass"
                  ],
                  "description": "Land-cover composition within one radius of the launch."
                },
                "description": "Composition discs, ascending radius."
              }
            },
            "required": [
              "source",
              "atLaunch",
              "fractions"
            ],
            "description": "What the ground around the launch is made of — the thermal-source character (forest holds heat back; clearcut, rock and grass release it; water kills it)."
          }
        },
        "required": [
          "elevation",
          "terrain",
          "landCover"
        ],
        "description": "One site's measured ground truth: the elevation pick, terrain analysis, and land cover. Coordinates and timezone are NOT echoed here — sites.json is their home; join by slug."
      },
      "description": "Site slug → context. Join against sites.json; slugs are the identity."
    }
  },
  "required": [
    "schemaVersion",
    "generatedAt",
    "sources",
    "sites"
  ],
  "description": "site-context.json — static per-site ground truth (the elevation pick, terrain, land cover), machine-measured from open data and committed beside the hand-maintained catalogues: humans author WHERE (sites.json); the pipeline measures WHAT (this file). No cadence: regenerate when the site catalogue changes."
}
